# deseq_helpers.R
# Helpers for DESeq2 model fitting and contrasts.

# TODO: add functions to:
# - build DESeqDataSet from counts + metadata
# - fit interaction models (region × time, genotype × time)
# - extract LRT/Wald results with consistent naming
