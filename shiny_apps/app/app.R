# app.R
# Minimal placeholder Shiny app.

library(shiny)

ui <- fluidPage(
  titlePanel("MouseBrainRhythmST"),
  p("TODO: replace with real UI." )
)

server <- function(input, output, session) {
  # TODO
}

shinyApp(ui, server)
